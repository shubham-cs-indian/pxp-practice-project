// TODO: #Refact20

/*
import Logger from './logger';

var LogFactory = (function () {

  return {
    getLogger: function (className) {
      return new Logger(className, null);
      // return new Logger(className, logWorker);
    }
  };
})();

export default LogFactory;*/
