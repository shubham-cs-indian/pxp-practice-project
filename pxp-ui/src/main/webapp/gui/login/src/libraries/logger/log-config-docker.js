// TODO: #Refact20

/*
/!**
 * Created by DEV on 26-06-2015.
 *!/
var MODE = {
  PRODUCTION: "production",
  DEVELOPMENT: "development"
};

var CONFIG = {
  IP_ADDRESS: "logserverip",
  SERVER_PORT: "logserverport",
  LOG_MODE: MODE.PRODUCTION,             //[production, development]
  DEBUG_MODE: false                        //[true, false]
};

export default {
  mode: MODE,
  config: CONFIG
};*/
