import {getTranslations} from "../store/helper/translation-manager";

let oActivities = {
  CREATED: {
    id: "CREATED",
    label: "CREATED",
  },
  DELETED: {
    id: "DELETED",
    label: "DELETED",
  },
  UPDATED: {
    id: "UPDATED",
    label: "UPDATED",
  },
};

export default oActivities;