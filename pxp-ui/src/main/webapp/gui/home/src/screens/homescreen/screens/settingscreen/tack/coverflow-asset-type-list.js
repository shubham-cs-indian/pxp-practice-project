/*import CS from '../../../../../libraries/cs';

var assetTypes = {
  FileImportTypes: [".xlsx", ".xls"]
};

assetTypes['allTypes'] = CS.reduce(assetTypes, function(all, type) {
  return all.concat(type);
}, []);
*/

const FileImportTypes = [".xlsx", ".xls"];
export default FileImportTypes;
